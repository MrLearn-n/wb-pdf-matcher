{
  "name": "wb-pdf-matcher",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w client run build && npm -w server run build",
    "start": "node server/dist/index.js",
    "import": "npm -w server run import"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
